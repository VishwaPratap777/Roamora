/* =====================================================
   Vercel Serverless Function — API Entry Point
   ===================================================== */

import app from '../server/src/index.js';

export default app;
